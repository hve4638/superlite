role: Tauri 데스크톱 껍데기 — front dist 를 자산으로 번들하고, relay serve 를 loopback 임의 포트 + 세션 토큰으로 in-process 기동, WS endpoint 를 webview 에 주입. 와이어 계약·daemon 분리 수명은 그대로 (Tauri IPC 전환은 비목표)

import { serve } from '../../backend/relay/src'
import { plain } from '../../backend/common/src'
