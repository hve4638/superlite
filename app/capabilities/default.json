{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "메인 창 기본 권한 — core 기본 묶음 (debug devtools 토글 포함)",
  "windows": ["main"],
  "permissions": ["core:default"]
}
