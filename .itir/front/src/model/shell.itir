role: 셸 선택 (ticket mobile-shell) — 뷰포트 폭이 아니라 명시적 진입 경로. `/mobile`(relay 라우트, 끝 슬래시 허용)·`/mobile.html`(vite dev)이면 모바일 셸, 그 밖은 데스크톱. host.ts 가 읽어 워크스페이스 상태 저장 키와 부팅 세션 출처(model/mobileSessions)를 가른다

export val mobileShell
