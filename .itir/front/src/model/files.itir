import { ctx, viewOf } from './ctx'
import { DirEntry, ThinBackend } from '../backend/types'

role: 탐색기 파일 트리 — lazy 디렉토리 로드, 외부 변경 시 로드된 디렉토리만 병합 리프레시. 세션별 팩토리(createFiles) + 활성 세션 전달 shim — UI 는 종전 이름 그대로 활성 세션에 작용한다

export type TreeNode
export val files

export fn createFiles(backend) {
  role: 세션별 트리 모듈 생성 — 트리·펼침·로딩 상태가 팩토리 클로저에 산다 (한 페이지에 세션 여럿). loading(루트 첫 로드, 진행 막대)·slowDirs(자식 로드 800ms 초과 디렉토리, twistie 스피너 — VS Code asyncDataTree slow 규칙)
}
export fn parentOf(path) {
  role: 부모 디렉토리 경로 (루트는 '') — 순수 함수, 세션 무관
}
export fn initFiles() {
  role: 루트 나열로 트리 초기화 — root 가 오면 loading 을 내리고(탐색기 진행 막대 종료) quick open 용 listFiles 는 그 뒤에. 셸은 이 완료를 기다리지 않고 마운트된다 (main.ts)
}
export fn toggleDir(node) {
  role: 디렉토리 펼침/접힘 (첫 펼침에 자식 로드)
}
export fn refreshDir(path) {
  role: 한 디렉토리 재나열 후 기존 노드와 병합 — 펼침 상태·로드된 자식 보존
}
export fn loadedDirPaths() -> Path[] {
  role: 로드된 디렉토리 경로 나열 (전체 리프레시 대상)
}
export fn refreshAllFiles() {
  role: Quick Open 파일 목록 재조회
}
export fn refreshTree() {
  role: 수동 새로고침 (탐색기 Refresh 버튼) — 로드된 디렉토리 재나열 + Quick Open 목록 재조회. 실패는 삼킨다 (워처·다음 시도가 복구)
}
export fn collapseAll() {
  role: 모두 접기 — 펼침 집합만 비운다. 로드된 자식은 유지되어 재펼침에 왕복이 없다
}
export fn revealPath(path) {
  role: 조상 디렉토리를 차례로 로드·펼치고 해당 경로를 선택 — 활성 편집기 자동 reveal(VS Code explorer.autoReveal)의 몸체. 트리에 없는 경로는 닿는 데까지만
}
export fn visibleNodes() -> TreeNode[] {
  role: 펼침 상태 기준 평탄화 — 렌더링용
}
